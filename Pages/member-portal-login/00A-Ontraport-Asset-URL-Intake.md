# Ontraport Asset URL Intake — Member Portal Login

## Instructions

1. Download each asset from `source/extracted-assets/` (also listed in `00-Assets-Needed.md`).
2. Upload it to Ontraport using the suggested filename.
3. Keep files organized by page and section.
4. Paste the direct Ontraport-hosted asset URL in the second column.
5. Do not paste an Ontraport editor, preview, or file-manager page URL.

| Section Name / Asset | Paste Ontraport URL |
|---|---|
| Stage — `member-portal-stage-bg.jpg` |  |
| Stage — `landmark-logo-white.png` |  |
| Stage (eyebrow font) — `tiempos-headline-light-italic.woff2` |  |

> The body font `objektiv-mk1` is loaded via Adobe Fonts (Typekit kit `gwi7lul`)
> and is **not** uploaded here — unless you choose to self-host it per the repo
> standard (see `00-Assets-Needed.md`).

## Agent Validation

- Upload path selected: **Continue with source URLs (embedded / Path B)** — upload optional/deferred
- All required URLs returned: No (pending)
- Direct hosted URLs verified: No (pending)
- Source URLs replaced in local files: No — markers `REPLACE_WITH_ONTRAPORT_URL/…` in place
- Source URLs replaced in master files: No — markers in `04-Master-Page.css`
- Remaining unresolved assets: 3 (stage background, logo, Tiempos Headline font)
